{
  "name": "server-bun",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch ./index.ts",
    "build": "npm run es-build && npm run bun-build",
    "es-build": "esbuild ./index.ts --platform=node --format=cjs --bundle --outfile=dist/index.js",
    "pkg-build": "pkg dist/index.js -t latest -o ../../src-tauri/bin/server-x86_64-pc-windows-msvc",
    "bun-build": "bun build --compile --minify --sourcemap ./index.ts --outfile ../../src-tauri/bin/server-x86_64-pc-windows-msvc"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/mime-types": "^2.1.4",
    "hono": "^4.4.5",
    "mime-types": "^2.1.35"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}